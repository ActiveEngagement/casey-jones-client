---
"casey-jones": minor
---

Ship generated TypeScript definitions at `resources/types/casey-jones.d.ts` (under the `Actengage.CaseyJones` namespace), produced from the Data objects and enums via spatie/typescript-transformer. Run `composer types` to regenerate; CI verifies the committed file stays in sync.
